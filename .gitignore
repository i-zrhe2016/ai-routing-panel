__pycache__/
*.py[cod]
.env
.env.ops-reporting

.venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/
*.egg-info/

# Built SPA release artifacts are deployment inputs and are intentionally not
# ignored.

# Local-only frontend toolchain output.
frontend/node_modules/
frontend/.vite/
node_modules/

# Local-only compose override (host-specific IPs, SSH mounts) — not for the repo.
docker-compose.override.yml

data/
logs/
backups/

dimt_v2/
app/xray/.env
app/xray/ai-proxy-outbound.json
!app/xray/logs/
app/xray/logs/**
!app/xray/logs/.gitkeep
app/xray/runtime/**
!app/xray/runtime/.gitkeep
!app/xray/reports/
app/xray/reports/**
!app/xray/reports/.gitkeep

# opencode local agent config (contains infra host IPs / key paths)
AGENTS.md

# stray env backups
.env.bak*
*.env.bak*
.env.backup-*
monitoring/.env
monitoring/prometheus/secrets/
monitoring/*.backup-*
monitoring/prometheus/*.backup-*

# Local Claude/runtime state and project-private instructions
.claude/
CLAUDE.md
.vite/
