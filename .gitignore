__pycache__/
*.py[cod]
.env
.env.ops-reporting

.venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/
*.egg-info/

# Built SPA bundles — produced by `npm run build` (and the Docker frontend-builder
# stage). Not committed; run the build for local non-Docker serving.
app/static/admin/
app/static/portal/
app/static/landing/

# Frontend toolchain deps/cache. The BUILT app/static/panel.js IS committed
# (runtime Docker has no build step), so do not ignore that.
frontend/node_modules/
frontend/.vite/
node_modules/

# Local-only compose override (host-specific IPs, SSH mounts) — not for the repo.
docker-compose.override.yml

data/
logs/
backups/

dimt_v2/
app/xray/.env
app/xray/ai-proxy-outbound.json
!app/xray/logs/
app/xray/logs/**
!app/xray/logs/.gitkeep
app/xray/runtime/**
!app/xray/runtime/.gitkeep
!app/xray/reports/
app/xray/reports/**
!app/xray/reports/.gitkeep

# opencode local agent config (contains infra host IPs / key paths)
AGENTS.md

# stray env backups
.env.bak*
*.env.bak*
.env.backup-*
monitoring/.env
monitoring/prometheus/secrets/
monitoring/*.backup-*
monitoring/prometheus/*.backup-*

# Local Claude/runtime state and project-private instructions
.claude/
CLAUDE.md
.vite/
